# Backend requirements for LOCAL DEVELOPMENT
# For Vercel deployment, use api/requirements.txt (no heavy ML deps)

# Core Flask dependencies
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0

# API clients
openai==1.12.0
google-cloud-vision==3.7.0
requests==2.31.0

# Media processing
yt-dlp==2025.10.14
Pillow==10.1.0
moviepy==1.0.3  # For local dev only (Vercel uses ffmpeg)

# Data processing
numpy==1.26.3
scikit-learn==1.4.0  # For local dev (Vercel uses numpy only)

# Web scraping
beautifulsoup4==4.12.3
lxml==5.1.0

# ML Dependencies (for CLIP visual similarity)
sentence-transformers==2.3.1
torch==2.8.0
torchvision==0.23.0

